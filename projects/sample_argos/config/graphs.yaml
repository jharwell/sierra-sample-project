intra-exp:
  LN_foraging:
    - src_stem: collected-data
      dest_stem: robot-counts
      cols:
        - walking
        - resting
      title: 'Robot Counts'
      legend:
        - 'Walking'
        - 'Resting'

      xlabel: 'Time'
      ylabel: '\# Robots'
      type: 'stacked_line'

    - src_stem: collected-data
      dest_stem: food-counts
      cols:
        - collected_food
      title: 'Collected Food Counts'
      legend:
        - ''

      xlabel: 'Time'
      ylabel: '\# Items'
      type: 'stacked_line'

    - src_stem: collected-data
      dest_stem: swarm-energy
      cols:
        - energy
      title: 'Swarm Energy Over Time'
      legend:
        - ''

      xlabel: 'Time'
      type: 'stacked_line'

inter-exp:
  HM_foraging:
    - src_stem: collected-data
      dest_stem: robot-counts-walking2
      col: walking
      title: 'Robot Walking Counts'
      type: 'heatmap'

    - src_stem: collected-data
      dest_stem: robot-counts-resting2
      col: resting
      title: 'Robot Resting Counts'
      type: 'heatmap'

    - src_stem: collected-data
      dest_stem: food-counts2
      col: collected_food
      title: 'Collected Food Counts'
      type: 'heatmap'

    - src_stem: collected-data
      dest_stem: swarm-energy2
      col: energy
      title: 'Swarm Energy Over Time'
      type: 'heatmap'

  LN_foraging:
    - src_stem: collected-data
      dest_stem: robot-counts-walking
      col: walking
      title: 'Robot Walking Counts'
      xlabel: 'Time'
      type: 'stacked_line'

    - src_stem: collected-data
      dest_stem: robot-counts-resting
      col: resting
      title: 'Robot Resting Counts'
      xlabel: 'Time'
      type: 'stacked_line'

    - src_stem: collected-data
      dest_stem: food-counts
      col: collected_food
      title: 'Collected Food Counts'
      xlabel: 'Time'
      type: 'stacked_line'

    - src_stem: collected-data
      dest_stem: swarm-energy
      col: energy
      title: 'Swarm Energy Over Time'
      xlabel: 'Time'
      type: 'stacked_line'

    - src_stem: collected-data
      dest_stem: swarm-energy-summary
      col: energy
      title: 'Swarm Energy Over Time'
      type: 'summary_line'

imagize:
    - src_stem: floor-state
      title: 'Floor State'
      type: 'heatmap'
